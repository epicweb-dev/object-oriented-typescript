# Method Overriding

<EpicVideo url="https://www.epicweb.dev/workshops/object-oriented-typescript/3-inheritance-and-polymorphism/method-overriding" />

👨‍💼 Excellent! Each shape now provides its own `getArea()` implementation.

🦉 Method overriding allows subclasses to provide specialized behavior while
maintaining the same method signature. Both `Circle` and `Rectangle` have
`getArea()`, but each calculates the area differently based on its shape.
