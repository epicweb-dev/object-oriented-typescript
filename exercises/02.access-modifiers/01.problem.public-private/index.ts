// Public and Private Access Modifiers

// 🐨 Create a User class with:
// - Public field: username (string)
// - Private field: password (string)
// - Constructor that takes username and password
// - Public method: login(password: string) returns boolean
//   - Returns true if password matches, false otherwise

// Test your class
// const user = new User('alice', 'secret123')
// console.log(user.username) // ✅ Should work
// console.log(user.password) // ❌ Should error - password is private
// console.log(user.login('secret123')) // ✅ Should return true
// console.log(user.login('wrong')) // ✅ Should return false

export {}
